---
phase: 02.57-p2-5-07-merge-items-bind-text
plan: 02
type: execute
wave: 2
depends_on: ['02.57-01']
files_modified:
  - frontend/src/canvas/text/text-wrap.ts
  - frontend/src/canvas/tools/select/group.ts
  - frontend/src/canvas/tools/select/merge.ts
  - frontend/src/canvas/shapes/text.tsx
  - frontend/src/canvas/tools/select/group-drag.ts
  - frontend/src/canvas/tools/select/pointer-move.ts
  - frontend/src/canvas/tools/select/pointer-up.ts
  - frontend/src/store/zorder.ts
  - frontend/src/canvas/text/__tests__/text-wrap.test.ts
  - frontend/src/canvas/tools/select/__tests__/group.test.ts
  - frontend/src/canvas/tools/select/__tests__/group-drag.test.ts
  - frontend/src/store/__tests__/zorder.test.ts
autonomous: true
requirements: [AC-4, AC-5, AC-6, AC-7, AC-12]
must_haves:
  truths:
    - "Merging exactly 1 text + 1 container-eligible shape centers the text in the container, wraps it to the container width with padding, and paints it above the container."
    - "Merging 1 text + 2+ containers, or 2+ texts, produces a plain group with no center/wrap/zIndex binding."
    - "Moving a bound container moves its bound text by the same delta; resizing recenters and rewraps the label; deleting the container also deletes the label."
    - "A z-order command on a bound container carries the bound text above it; the same command applied directly to the bound text is a no-op."
    - "Each bound-pair operation is a single undo/redo step."
  artifacts:
    - frontend/src/canvas/text/text-wrap.ts
    - frontend/src/canvas/tools/select/group-drag.ts
    - frontend/src/canvas/text/__tests__/text-wrap.test.ts
    - frontend/src/canvas/tools/select/__tests__/group-drag.test.ts
  key_links:
    - "resolveGroupBinding (group.ts) is the SINGLE source of truth for bound-pair detection, reused by merge/render/cascade/zorder"
    - "computeBoundTextLayout (text-wrap.ts) with injectable measure fn → jsdom-safe (RESEARCH Pitfall 1)"
    - "onMergeSelected bound-layout patch + zorder cascade both go through ONE updateElements call (AC-12)"
---

<objective>
Deliver text-binding at merge time — slice (b) of the D-04 delivery. When a merge produces exactly
one `text` + one container-eligible shape, the text becomes a centered, width-wrapped label painted
above the container, and it follows the container on move / resize / delete / z-order. Binding is a
DERIVED relationship (RESEARCH Pitfall 2): there is no stored `boundContainerId` flag — every
consumer re-derives it from `groupId` + type composition through one shared selector
`resolveGroupBinding`, satisfying AC-13 (no new sync slot).

Purpose: Add the label-binding special case on top of slice (a)'s grouping, with a jsdom-safe
text-wrap module (RESEARCH Pitfall 1: canvas 2D context is null under Vitest).
Output: `text-wrap.ts`, `resolveGroupBinding` in `group.ts`, bound-layout at merge, wrapped SVG
render, container→text move/resize cascade (`group-drag.ts`), and z-order propagation (`zorder.ts`).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.57-p2-5-07-merge-items-bind-text/02.57-CONTEXT.md
@.planning/phases/02.57-p2-5-07-merge-items-bind-text/02.57-RESEARCH.md
@.planning/phases/02.57-p2-5-07-merge-items-bind-text/02.57-PATTERNS.md
@.planning/phases/02.57-p2-5-07-merge-items-bind-text/02.57-UI-SPEC.md
@specs/048-merge-items-bind-text/acceptance.md
@.planning/phases/02.57-p2-5-07-merge-items-bind-text/02.57-01-SUMMARY.md
</context>

## Artifacts this phase produces (slice b)

| Symbol / File | Kind | Signature / Shape |
|---|---|---|
| `frontend/src/canvas/text/text-wrap.ts` | new file | pure, jsdom-safe |
| `measureTextWidth` | exported fn | `(text: string, font: string, measure?: MeasureFn) => number` — default uses canvas 2D when a non-null context exists, else a deterministic per-char fallback; `MeasureFn = (text: string, font: string) => number` |
| `wrapText` | exported fn | `(text: string, maxWidth: number, font: string, measure?: MeasureFn) => string[]` — greedy word-wrap, preserves explicit `\n`, never returns 0 lines |
| `computeBoundTextLayout` | exported fn | `(container: Element, textEl: Element, measure?: MeasureFn) => { x: number; y: number; width: number; height: number; lines: string[]; lineHeight: number }` — inset `container.width - 16`, clamp width floor `24`, vertical block centering with `lineHeight = fontSize * 1.2` |
| `getBoundTextLines` | exported fn | `(element: Element, elements: Element[], measure?: MeasureFn) => { lines: string[]; lineHeight: number; textX: number; firstLineY: number } | null` — returns layout only when `element` is the bound text of its group, else null |
| `resolveGroupBinding` (in group.ts) | exported fn | `(groupId: string, elements: Element[]) => GroupBinding \| null` — `{ textId, containerId }` only when exactly 1 text + 1 container-eligible member |
| `GroupBinding` (in group.ts) | exported interface | `{ textId: string; containerId: string }` |
| `computeBoundContainerCascade` (in group-drag.ts) | exported fn | `(containerDraft: Element, allElements: Element[], measure?: MeasureFn) => Element[]` — returns the bound-text follow draft(s) for a moved/resized container (delta-move on move, relayout on resize); `[]` when the container is not part of a bound pair |

> `resolveGroupMembers`, `CONTAINER_TYPES`, `resolveGroupDeletionIds` already exist from slice (a).
> Do not add a `boundContainerId` / `isBoundText` field to Element/ElementProps (AC-13, Pitfall 2).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: text-wrap.ts (jsdom-safe) + resolveGroupBinding + Wave 0 tests</name>
  <files>
    frontend/src/canvas/text/text-wrap.ts (new),
    frontend/src/canvas/tools/select/group.ts (modify),
    frontend/src/canvas/text/__tests__/text-wrap.test.ts (new),
    frontend/src/canvas/tools/select/__tests__/group.test.ts (modify)
  </files>
  <read_first>
    - frontend/src/canvas/shapes/text.tsx (lineHeight = fontSize * 1.2 L12; textAnchor/textX centering L13-20; tspan-per-line render L23-44)
    - frontend/src/canvas/tools/select/group.ts (from slice a: CONTAINER_TYPES, resolveGroupMembers — resolveGroupBinding is ADDED here)
    - frontend/src/canvas/tools/select/bound-arrows.ts (filter/map pure idiom to mirror for resolveGroupBinding)
    - frontend/src/test/setup.ts (ONLY a ResizeObserver polyfill — NO canvas getContext mock: confirms Pitfall 1)
    - frontend/vite.config.ts (test.environment: 'jsdom')
    - packages/shared/src/index.ts (Element/ElementProps: fontSize L28, fontFamily L29, textAlign L30, text L27)
    - .planning/phases/02.57-p2-5-07-merge-items-bind-text/02.57-RESEARCH.md (Pitfall 1 injectable measure; Pattern 1; text-wrap primary recommendation; A2 flat inset; A3 vertical centering)
    - .planning/phases/02.57-p2-5-07-merge-items-bind-text/02.57-UI-SPEC.md (Bound Text Layout: 8px padding → width `container.width - 16`, 24px floor, center h+v)
  </read_first>
  <behavior>
    measureTextWidth: given an injected measure fn returning 10 per char, `'abc'` → 30; with no context available (jsdom default) it does NOT throw and returns a finite positive number via the fallback. @covers AC-4
    wrapText: text whose words exceed maxWidth splits into multiple lines at word boundaries; an explicit `\n` forces a new line; a single word longer than maxWidth still yields at least one line (no crash, no empty array). @covers AC-4
    computeBoundTextLayout: width = `container.width - 16`, clamped to >= 24; `x` centers the block horizontally in the container; total block height = `lines.length * lineHeight` with `lineHeight = fontSize * 1.2`; `y`/firstLineY vertically centers the block within `container.height`. @covers AC-4
    resolveGroupBinding: group with exactly 1 text + 1 rectangle → `{ textId, containerId }`; group with 1 text + 2 rectangles → null; group with 2 texts + 1 rectangle → null; group with 1 text + 1 arrow (arrow NOT container-eligible) → null. @covers AC-5
    getBoundTextLines: returns layout for the text element of a bound pair; returns null for the container element, for an unbound text, and for a plain-group text. @covers AC-4 AC-5
  </behavior>
  <action>
    Create `frontend/src/canvas/text/text-wrap.ts` as a pure module. Define
    `type MeasureFn = (text: string, font: string) => number`. `measureTextWidth(text, font, measure?)`
    uses `measure` when provided; otherwise lazily creates a canvas 2D context and, ONLY if
    `getContext('2d')` returns non-null, calls `ctx.measureText(text).width`; when the context is null
    (jsdom) it falls back to a deterministic estimate (e.g. char-count × a fraction of the parsed
    font-size) — NEVER call `.measureText` on a null context (Pitfall 1). `wrapText(text, maxWidth,
    font, measure?)` does greedy word wrapping (split on spaces, accumulate until the next word would
    exceed maxWidth, preserve explicit `\n` boundaries, and guarantee at least one line even for an
    over-long single word). `computeBoundTextLayout(container, textEl, measure?)` builds the font
    string from `textEl.props.fontSize ?? 16` / `textEl.props.fontFamily ?? 'sans-serif'` (do NOT hard-code
    a font — Pitfall / anti-pattern), computes wrap width `Math.max(24, container.width - 16)`, wraps,
    and returns geometry that centers the block horizontally and vertically inside the container using
    the existing `lineHeight = fontSize * 1.2` convention. `getBoundTextLines(element, elements, measure?)`
    calls `resolveGroupBinding(element.groupId, elements)` and returns layout only when
    `binding?.textId === element.id`, else null.

    In `group.ts`, ADD `export interface GroupBinding { textId: string; containerId: string }` and
    `resolveGroupBinding(groupId, elements)`: `resolveGroupMembers` → partition into texts
    (`type === 'text'`) and containers (`CONTAINER_TYPES.has(el.type)`); return null unless
    `texts.length === 1 && containers.length === 1`; else `{ textId: texts[0].id, containerId: containers[0].id }`.
    Keep it pure (no store import), exactly RESEARCH Pattern 1.

    Create `text-wrap.test.ts` injecting a deterministic `MeasureFn` (per Pitfall 1 — DO NOT rely on
    jsdom canvas) covering the measure/wrap/layout behaviors, plus one test asserting that calling the
    default (no measure) path does not throw under jsdom. Extend `group.test.ts` with the four
    `resolveGroupBinding` cases and the `getBoundTextLines` null/non-null cases (AC-5).
  </action>
  <acceptance_criteria>
    - `text-wrap.ts` never calls `.measureText` on a null context — the default path is null-guarded and the tests inject a measure fn. @covers AC-4
    - `wrapText` returns >= 1 line for any non-empty input and splits at word boundaries; explicit `\n` preserved. @covers AC-4
    - `computeBoundTextLayout` clamps wrap width to a 24 floor and uses `lineHeight = fontSize * 1.2`. @covers AC-4
    - `resolveGroupBinding` returns a binding ONLY for exactly 1 text + 1 container-eligible member; null for 1-text/2-container and 2-text/1-container. @covers AC-5
    - `group.ts` still imports no store module. @covers AC-13
    - Test titles tagged `@covers AC-4` / `@covers AC-5`.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter whiteboard-fe test -- src/canvas/text/__tests__/text-wrap.test.ts src/canvas/tools/select/__tests__/group.test.ts</automated>
  </verify>
  <done>Wrap/measure/layout math is jsdom-safe and tested; resolveGroupBinding correctly distinguishes bound pair vs plain group.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bind at merge + render wrapped label + move/resize cascade</name>
  <files>
    frontend/src/canvas/tools/select/merge.ts (modify),
    frontend/src/canvas/shapes/text.tsx (modify),
    frontend/src/canvas/tools/select/group-drag.ts (new),
    frontend/src/canvas/tools/select/pointer-move.ts (modify),
    frontend/src/canvas/tools/select/pointer-up.ts (modify),
    frontend/src/canvas/tools/select/__tests__/group-drag.test.ts (new)
  </files>
  <read_first>
    - frontend/src/canvas/tools/select/merge.ts (from slice a: onMergeSelected single updateElements call)
    - frontend/src/canvas/text/text-wrap.ts (from Task 1: computeBoundTextLayout, getBoundTextLines)
    - frontend/src/canvas/tools/select/group.ts (resolveGroupBinding, resolveGroupMembers)
    - frontend/src/canvas/shapes/text.tsx (render: textX/textAnchor L13-20, tspan map L39-43 — extend to render wrapped bound-text lines)
    - frontend/src/canvas/tools/select/bound-arrows.ts (computeBoundArrowDrafts L14-40 — the "recompute referencing element" analog for computeBoundContainerCascade)
    - frontend/src/canvas/tools/select/pointer-move.ts (single-drag else-branch L152-164 and resize branches L79-151 where computeBoundArrowDrafts drafts are added)
    - frontend/src/canvas/tools/select/pointer-up.ts (draftElements commit branches: multi L~; single-with-bound-drafts commit via updateElements)
    - frontend/src/store/elements.store.ts (non-reactive getState read used by text.tsx render)
    - frontend/src/store/mutation-pipeline.ts (updateElements one-batch contract)
    - .planning/phases/02.57-p2-5-07-merge-items-bind-text/02.57-UI-SPEC.md (Bound Text Layout: zIndex above container, center h+v, font unchanged)
    - .planning/phases/02.57-p2-5-07-merge-items-bind-text/02.57-RESEARCH.md (Pattern 1 reuse everywhere; Pitfall 2 derived-not-stored)
  </read_first>
  <behavior>
    On merge of exactly 1 text + 1 container: the SAME updateElements batch that assigns groupId also
    sets the text element's textAlign to center, its x/y/width/height to the container inset box
    (via computeBoundTextLayout), and its zIndex above the container's zIndex. @covers AC-4
    Rendering: a bound text element renders its wrapped lines centered within the container box; an
    unbound text element renders unchanged (raw `\n` split). @covers AC-4
    computeBoundContainerCascade: given a container draft moved by (dx,dy), returns its bound text draft
    translated by the same delta; given a container draft resized, returns the bound text relaid-out to
    the new container box; returns [] when the container has no bound text. @covers AC-6
    Dragging a bound container updates the bound text in the draft layer and commits both in ONE
    updateElements batch; deleting the container deletes the label (covered by slice a group-delete
    since they share groupId). @covers AC-6 AC-12
  </behavior>
  <action>
    Extend `onMergeSelected` (merge.ts): after computing the groupId patches, call
    `resolveGroupBinding(groupId, elementsAfterMerge)` (evaluate against the selection's post-merge
    composition — i.e. the non-locked selected members with the new groupId). If it returns a binding,
    compute `computeBoundTextLayout(container, text)` and MERGE the text's layout patch
    (`x, y, width, height, props.textAlign = 'center', zIndex = container.zIndex + 1`) into the SAME
    patch array so the whole merge remains ONE updateElements call (AC-12). If null (plain group per
    AC-5), emit only the groupId patches — no layout, no zIndex change.

    Extend `text.tsx` render: call `getBoundTextLines(element, useElementsStore.getState().elements)`
    (a NON-reactive read — the renderer never owns state; the per-element `memo` re-renders because the
    cascade keeps the text element's own geometry synced). When it returns lines, render them as the
    centered/vertically-centered `<tspan>` block using the returned `textX`/`firstLineY`/`lineHeight`;
    when null, keep the existing `\n`-split render path unchanged. Do NOT change font-size/family
    (UI-SPEC Typography contract).

    Create `group-drag.ts` with `computeBoundContainerCascade(containerDraft, allElements, measure?)`
    modeled on `computeBoundArrowDrafts`: resolve the container's binding; if bound, produce the text
    follow draft — for a pure move (width/height unchanged vs the stored container) translate the text
    by the same delta; for a resize (dimensions changed) recompute the text box via
    `computeBoundTextLayout(containerDraft, textEl)`. Return [] when not a bound pair.

    Wire it into `pointer-move.ts`: in the single-element drag `else` branch (L152-164) and the resize
    branches, append `computeBoundContainerCascade(draftEl, elements)` results to the `setDraftElements`
    array ALONGSIDE the existing `computeBoundArrowDrafts` output (both dependents of the dragged
    element). Confirm `pointer-up.ts` already commits `draftElements` via `updateElements` in one call;
    if the bound-text draft is not covered by an existing commit branch, extend that branch so the
    label commit lands in the SAME batch as the container (one undo step).

    Create `group-drag.test.ts` covering AC-6: move delta propagation, resize relayout, and the
    single-batch commit assertion (spy on `updateElements`).
  </action>
  <acceptance_criteria>
    - Merge of 1 text + 1 container emits a single updateElements call that includes the text's centered geometry, `textAlign: 'center'`, and a zIndex above the container. @covers AC-4 AC-12
    - Merge of 1 text + 2 containers (or 2 texts + 1 container) emits groupId-only patches, no layout/zIndex change. @covers AC-5
    - `text.tsx` renders wrapped centered lines for a bound text and leaves unbound text rendering unchanged (grep: bound branch guarded by `getBoundTextLines`). @covers AC-4
    - `computeBoundContainerCascade` returns the follow draft for move and resize and `[]` for a non-bound container. @covers AC-6
    - Dragging a bound container commits container + label in ONE `updateElements` call. @covers AC-6 AC-12
    - Deleting a bound container also removes the label (via slice-a group-delete; assert in test). @covers AC-6
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter whiteboard-fe test -- src/canvas/tools/select/__tests__/group-drag.test.ts && pnpm --filter whiteboard-fe typecheck</automated>
  </verify>
  <done>Merging 1 text + 1 container binds a centered wrapped label above the shape that follows move/resize/delete as one undo step.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Z-order propagation to bound text (AC-7) + AC-12 confirmation</name>
  <files>
    frontend/src/store/zorder.ts (modify),
    frontend/src/store/__tests__/zorder.test.ts (modify)
  </files>
  <read_first>
    - frontend/src/store/zorder.ts (bringToFront L5-15, sendToBack L18-28, bringForward L31-45, sendBackward L48-62 — all single-element, all one updateElements call; note the existing two-patch batching in bringForward/sendBackward)
    - frontend/src/canvas/tools/select/group.ts (resolveGroupBinding)
    - frontend/src/store/__tests__/zorder.test.ts (existing seedStore + updateElements spy pattern to extend)
    - frontend/src/store/mutation-pipeline.ts (updateElements one-batch contract)
    - .planning/phases/02.57-p2-5-07-merge-items-bind-text/02.57-UI-SPEC.md (Bound Text Layout z-index: label always above its own container, including after any z-order command)
    - .planning/phases/02.57-p2-5-07-merge-items-bind-text/02.57-PATTERNS.md (zorder.ts MODIFIED section)
  </read_first>
  <behavior>
    bringToFront/sendToBack/bringForward/sendBackward on a bound CONTAINER also patch the bound text so
    it stays above the container's new zIndex, in ONE updateElements call. @covers AC-7 AC-12
    The same four commands invoked directly on a bound TEXT element are a no-op (early return, no
    updateElements call) because the label's z-position is derived from its container. @covers AC-7
    The four commands on an ungrouped / plain-group element behave exactly as before (regression). @covers AC-7
  </behavior>
  <action>
    In each of `bringToFront`/`sendToBack`/`bringForward`/`sendBackward`, after resolving the `target`,
    add two branches using `resolveGroupBinding(target.groupId, active)` (only when `target.groupId`):
    (1) if the target IS the binding's `textId`, return immediately (no-op — mirrors the existing
    "already at top/bottom" early-return idiom) so a z-order command on the label does nothing;
    (2) if the target IS the binding's `containerId`, compute the container's new zIndex as today, then
    EXTEND the patch array passed to the single `updateElements(...)` call with a patch that sets the
    bound text's zIndex to one above the container's resulting zIndex. Preserve the existing
    single-element behavior untouched when there is no binding. Keep exactly one `updateElements` call
    per command (AC-12).

    Extend `zorder.test.ts` with: (a) each command on a bound container also raises the label above it
    in one call; (b) each command directly on the bound text is a no-op (spy shows zero updateElements
    calls); (c) an existing single-element case still passes (regression).
  </action>
  <acceptance_criteria>
    - Each z-order command on a bound container includes a bound-text zIndex patch in the same single updateElements call, keeping the label above the container. @covers AC-7 AC-12
    - Each z-order command directly on bound text makes zero updateElements calls (no-op). @covers AC-7
    - Existing zorder.test.ts single-element cases still pass. @covers AC-7
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter whiteboard-fe test -- src/store/__tests__/zorder.test.ts</automated>
  </verify>
  <done>Z-order on a bound container carries the label above it as one undo step; z-order on the label is a no-op.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Bound-text geometry/zIndex/textAlign updates flow through the existing `updateElements` path and existing element slots. Binding is derived at read time — no new persisted field crosses any boundary (AC-13). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02.57-02 | Denial of Service | `text-wrap.ts` measure/wrap under jsdom | low | mitigate | Null-guard canvas 2D context + injectable measure fn so wrap never throws/hangs in tests (RESEARCH Pitfall 1). |
| T-02.57-SC | Tampering | npm/pip/cargo installs | n/a | accept | No packages installed this phase (RESEARCH Package Legitimacy Audit: not applicable). |
</threat_model>

<verification>
- `pnpm --filter whiteboard-fe test -- src/canvas/text/__tests__/text-wrap.test.ts src/canvas/tools/select/__tests__/group.test.ts src/canvas/tools/select/__tests__/group-drag.test.ts src/store/__tests__/zorder.test.ts` all green.
- Full slice gate: `pnpm --filter whiteboard-fe test` green before advancing to slice (c).
- `pnpm --filter whiteboard-fe typecheck` clean.
- Manual (UI-SPEC): merge 1 text + 1 rectangle/ellipse/diamond/triangle/polygon/image; confirm label reads centered, wraps to width, sits above the shape, and follows move/resize/delete/z-order.
</verification>

<success_criteria>
- 1 text + 1 container merge binds a centered, width-wrapped, higher-zIndex label (AC-4).
- Any other text/container composition produces a plain group (AC-5).
- Bound text follows container move (delta), resize (recenter/rewrap), and delete (AC-6).
- Z-order on container carries the label; z-order on the label is a no-op (AC-7).
- Every bound-pair operation is a single undo/redo step (AC-12).
- Binding is derived — no new field/slot/event added (AC-13 preserved).
</success_criteria>

<output>
Create `.planning/phases/02.57-p2-5-07-merge-items-bind-text/02.57-02-SUMMARY.md` when done.
</output>
