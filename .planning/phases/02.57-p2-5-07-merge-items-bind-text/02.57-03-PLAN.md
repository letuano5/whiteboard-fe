---
phase: 02.57-p2-5-07-merge-items-bind-text
plan: 03
type: execute
wave: 3
depends_on: ['02.57-02']
files_modified:
  - frontend/src/canvas/tools/select/group-drag.ts
  - frontend/src/canvas/tools/select/group-resize.ts
  - frontend/src/canvas/tools/select/pointer-down.ts
  - frontend/src/canvas/tools/select/pointer-move.ts
  - frontend/src/canvas/tools/select/pointer-up.ts
  - frontend/src/store/interaction.store.ts
  - frontend/src/canvas/layers/svg/SelectionOverlay.tsx
  - frontend/src/canvas/layers/svg/LocalSelectionLayer.tsx
  - frontend/src/canvas/hooks/use-whiteboard-pointer-handlers.ts
  - frontend/src/canvas/tools/select/__tests__/group-drag.test.ts
  - frontend/src/canvas/tools/select/__tests__/group-resize.test.ts
autonomous: true
requirements: [AC-8, AC-9, AC-12, AC-13]
must_haves:
  truths:
    - "Clicking any element that belongs to a group selects the whole group with a combined bbox and resize handles (D-01)."
    - "Dragging any single group member moves every non-locked member together in draft, committed as one mutation batch."
    - "Resizing a group by its combined bbox scales non-text members, recenters/rewraps container-bound text, and translates independent text without changing its fontSize (D-03)."
    - "Locked group members stay in place during group drag and group resize (D-02)."
    - "Group move and group resize are each a single undo/redo step, and no new WS event or sync slot was introduced (AC-13)."
  artifacts:
    - frontend/src/canvas/tools/select/group-resize.ts
    - frontend/src/canvas/tools/select/__tests__/group-resize.test.ts
    - frontend/src/canvas/tools/select/__tests__/group-drag.test.ts
  key_links:
    - "pointer-down D-01 group resolution → setSelectedIds(all members) → group overlay + group drag/resize"
    - "computeGroupDragDrafts / computeGroupResizeDrafts → setDraftElements → pointer-up ONE updateElements commit (AC-12)"
    - "getMultiSelectBounds reused for group bbox (no second bbox fn)"
---

<objective>
Deliver group-bbox resize, full group-drag, and D-01 group-click selection — slice (c), the final
slice of the D-04 delivery. Clicking any grouped element selects the whole group (combined bbox +
resize handles); dragging any member moves the whole group; resizing the group bbox scales
non-text members, recenters/rewraps bound text, and translates independent group text WITHOUT
scaling its fontSize (D-03). All of it batches through the existing mutation pipeline as one
undoable step and introduces no new WS event or sync slot (AC-13).

Purpose: Complete the "merge items" operational unit so a merged group behaves like a single object
for selection, drag, and bbox-resize, closing AC-8, AC-9, AC-12, AC-13.
Output: `computeGroupDragDrafts` (group-drag.ts), `group-resize.ts`, D-01 selection resolution in
pointer-down, a handled group-selection overlay, group-resize session wiring, and the AC-13
regression check.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.57-p2-5-07-merge-items-bind-text/02.57-CONTEXT.md
@.planning/phases/02.57-p2-5-07-merge-items-bind-text/02.57-RESEARCH.md
@.planning/phases/02.57-p2-5-07-merge-items-bind-text/02.57-PATTERNS.md
@.planning/phases/02.57-p2-5-07-merge-items-bind-text/02.57-UI-SPEC.md
@specs/048-merge-items-bind-text/acceptance.md
@.planning/phases/02.57-p2-5-07-merge-items-bind-text/02.57-01-SUMMARY.md
@.planning/phases/02.57-p2-5-07-merge-items-bind-text/02.57-02-SUMMARY.md
</context>

## Artifacts this phase produces (slice c)

| Symbol / File | Kind | Signature / Shape |
|---|---|---|
| `computeGroupDragDrafts` (in group-drag.ts) | exported fn | `(groupMemberIds: string[], dx: number, dy: number, allElements: Element[], measure?: MeasureFn) => Element[]` — translates NON-locked members (D-02) by (dx,dy), plus bound-text/bound-arrow dependents; one flat array |
| `frontend/src/canvas/tools/select/group-resize.ts` | new file | pure transform |
| `computeGroupResizeDrafts` | exported fn | `(groupMemberIds: string[], oldBounds: MultiSelectBounds, newBounds: MultiSelectBounds, allElements: Element[], measure?: MeasureFn) => Element[]` — scales non-text members; recenters/rewraps container-bound text; translates independent text position-only (NO fontSize change, D-03); skips locked members (D-02) |
| `resolveSelectionGroupIds` (in group.ts, reused) | helper | resolves whether a selection is a single-group selection to drive overlay + drag/resize |
| group-resize session | interaction.store field | mirrors `resizeSession`; tracks old group bbox + active handle |
| `GroupSelectionOverlay` (SelectionOverlay.tsx) | exported component | outline (`#3b82f6`, dashed) + 8 resize handles (`r=4`, no rotate handle) over the group bbox |

> `computeGroupDragDrafts` is ADDED to the existing `group-drag.ts` from slice (b) (which already
> holds `computeBoundContainerCascade`). Do not duplicate the bound-text follow logic — reuse it.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: D-01 group-click selection + full group drag (AC-8)</name>
  <files>
    frontend/src/canvas/tools/select/pointer-down.ts (modify),
    frontend/src/canvas/tools/select/pointer-move.ts (modify),
    frontend/src/canvas/tools/select/group-drag.ts (modify),
    frontend/src/canvas/tools/select/__tests__/group-drag.test.ts (modify)
  </files>
  <read_first>
    - frontend/src/canvas/tools/select/pointer-down.ts (replace-selection else-branch L44-53; isFullyBoundArrow guard precedent L38/47; hit-test loop L24-56)
    - frontend/src/canvas/tools/select/pointer-move.ts (multi-drag branch L37-45 gated on `selectedIds.length > 1`)
    - frontend/src/canvas/tools/select/bound-arrows.ts (computeMultiDragDrafts two-phase pattern L43-96 — the model for computeGroupDragDrafts)
    - frontend/src/canvas/tools/select/group-drag.ts (from slice b: computeBoundContainerCascade to reuse for bound-text dependents)
    - frontend/src/canvas/tools/select/group.ts (resolveGroupMembers; add a selection→group resolver if needed)
    - frontend/src/canvas/tools/select/pointer-up.ts (draftElements commit branch that fires one updateElements)
    - frontend/src/canvas/tools/text-editor.tsx (existing double-click text-edit path — must keep precedence for grouped text; find the double-click handler)
    - .planning/phases/02.57-p2-5-07-merge-items-bind-text/02.57-CONTEXT.md (D-01 group-click; D-02 locked exclusion)
    - .planning/phases/02.57-p2-5-07-merge-items-bind-text/02.57-RESEARCH.md (Open Question 1: double-click drill-in; Pitfall 3 locked)
  </read_first>
  <behavior>
    Clicking a grouped element (single click, no shift) sets selectedIds to ALL members of that group
    (including a locked member so it appears in the combined bbox). @covers AC-8 (D-01)
    Dragging any single grouped member moves every NON-locked member of the group together in the draft
    layer; a locked member stays in place. @covers AC-8 (D-02)
    The group move commits as ONE updateElements batch through pointer-up. @covers AC-8 AC-12
    Double-click on a grouped NON-text member drills in to select just that member; double-click on a
    grouped text member still enters text-edit mode (existing behavior preserved). @covers AC-8 (D-01 drill-in)
    A bound container inside the dragged group carries its bound text (reusing computeBoundContainerCascade). @covers AC-8
  </behavior>
  <action>
    In `pointer-down.ts`, in the non-shift replace-selection branch (L44-53): if the hit element has a
    non-null `groupId`, resolve `resolveGroupMembers(el.groupId, elements)` and
    `setSelectedIds(members.map((m) => m.id))` instead of `[el.id]`; still start the drag from the hit
    element (guarded by `isFullyBoundArrow`). Keep ungrouped behavior identical. Add double-click
    drill-in: when a double-click lands on a grouped NON-text member, select only that member (clear the
    group selection); route grouped TEXT double-click to the existing text-edit handler unchanged (do
    not steal it) — locate the current double-click handler and branch on element type + groupId.

    Add `computeGroupDragDrafts(groupMemberIds, dx, dy, allElements, measure?)` to `group-drag.ts`
    modeled on `computeMultiDragDrafts` (two-phase: translate members, then dependents). Phase 1
    translates ONLY non-locked members (`.filter((el) => !el.locked)` — D-02, Pitfall 3). Phase 2
    collects dependents: bound arrows (reuse the existing bound-arrow lookup) AND bound-text via the
    slice-(b) `computeBoundContainerCascade` for any bound container in the moved set. Return one flat
    `Element[]`.

    In `pointer-move.ts`, make the drag branch group-aware: when the current selection resolves to a
    single shared group, call `computeGroupDragDrafts(memberIds, dx, dy, allElements)` and
    `setDraftElements(...)`. This must trigger even when only one member is under the pointer — so the
    branch condition changes from the bare `selectedIds.length > 1` to also cover "selection is a group"
    (call this out explicitly; do not silently keep the old gate). Non-group multi-select keeps using
    `computeMultiDragDrafts`. Confirm `pointer-up.ts` commits these drafts in one `updateElements` call.

    Extend `group-drag.test.ts` with AC-8: all non-locked members move by delta; locked member excluded;
    one commit batch (spy on updateElements); bound container carries its text.
  </action>
  <acceptance_criteria>
    - Clicking a grouped element selects all members (assert selectedIds contains every member id). @covers AC-8
    - `computeGroupDragDrafts` translates non-locked members by (dx,dy) and excludes locked members. @covers AC-8 (D-02)
    - Group drag commits exactly one `updateElements` call. @covers AC-8 AC-12
    - Double-click on grouped text still enters edit mode (existing text-edit path untouched); double-click on grouped non-text selects the single member. @covers AC-8 (D-01)
    - `pnpm --filter whiteboard-fe typecheck` passes.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter whiteboard-fe test -- src/canvas/tools/select/__tests__/group-drag.test.ts && pnpm --filter whiteboard-fe typecheck</automated>
  </verify>
  <done>Clicking a grouped element selects the whole group; dragging any member moves all non-locked members as one undo step.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Group-bbox resize (AC-9) + handled group overlay</name>
  <files>
    frontend/src/canvas/tools/select/group-resize.ts (new),
    frontend/src/store/interaction.store.ts (modify),
    frontend/src/canvas/layers/svg/SelectionOverlay.tsx (modify),
    frontend/src/canvas/layers/svg/LocalSelectionLayer.tsx (modify),
    frontend/src/canvas/hooks/use-whiteboard-pointer-handlers.ts (modify),
    frontend/src/canvas/tools/select/pointer-move.ts (modify),
    frontend/src/canvas/tools/select/pointer-up.ts (modify),
    frontend/src/canvas/tools/select/__tests__/group-resize.test.ts (new)
  </files>
  <read_first>
    - frontend/src/canvas/layers/svg/selectors.ts (getMultiSelectBounds L89-109 — reuse for group bbox, pass member ids)
    - frontend/src/canvas/tools/select/resize.ts (why NOT to reuse the anchor/handle session — Pitfall 4; new bbox linear transform instead)
    - frontend/src/canvas/text/text-wrap.ts (computeBoundTextLayout to recenter/rewrap bound text on resize)
    - frontend/src/canvas/tools/select/group.ts (resolveGroupMembers, resolveGroupBinding, CONTAINER_TYPES)
    - frontend/src/canvas/layers/svg/SelectionOverlay.tsx (single-element handles spec r=4/fill white/stroke #3b82f6 L91-104; MultiSelectionOverlay outline-only L125-139; rotate handle to OMIT L105-116)
    - frontend/src/canvas/layers/svg/LocalSelectionLayer.tsx (getMultiSelectBounds render path L38-48)
    - frontend/src/canvas/hooks/use-whiteboard-pointer-handlers.ts (handleHandlePointerDown L278-291; onSelectHandlePointerDown wiring)
    - frontend/src/canvas/tools/select/pointer-down.ts (onSelectHandlePointerDown resize-session setup L67-103 — analog for group-resize session)
    - frontend/src/store/interaction.store.ts (resizeSession field shape to mirror for a group-resize session)
    - frontend/src/canvas/tools/select/pointer-up.ts (draftElements commit branch)
    - .planning/phases/02.57-p2-5-07-merge-items-bind-text/02.57-CONTEXT.md (D-03 independent text position-only, no fontSize scale)
    - .planning/phases/02.57-p2-5-07-merge-items-bind-text/02.57-UI-SPEC.md (Group Selection Overlay: 8 handles, NO rotate, exact handle visual spec; scope only when selection resolves to a groupId)
  </read_first>
  <behavior>
    computeGroupResizeDrafts scales each NON-text, non-locked member's x/y/width/height proportionally
    from oldBounds to newBounds. @covers AC-9
    A container-bound text member is recentered/rewrapped to its resized container via computeBoundTextLayout
    (not scaled by raw bbox ratio). @covers AC-9
    An independent group text member (no binding) has ONLY its x/y translated/scaled to the new bbox;
    its fontSize is unchanged. @covers AC-9 (D-03)
    A locked member is left unchanged (D-02). @covers AC-9 (D-02)
    Group resize commits as ONE updateElements batch. @covers AC-9 AC-12
    The group overlay renders 8 resize handles and NO rotate handle only when the selection resolves to
    a shared group. @covers AC-9 (D-01)
  </behavior>
  <action>
    Create `group-resize.ts` with `computeGroupResizeDrafts(groupMemberIds, oldBounds, newBounds,
    allElements, measure?)`. For each non-locked member (skip locked — D-02): compute scale factors
    `sx = newBounds.width / oldBounds.width`, `sy = newBounds.height / oldBounds.height`; map the
    member's top-left relative to `oldBounds` into `newBounds`. Branch by member role using
    `resolveGroupBinding`: (a) non-text members scale x/y/width/height (and point geometry if
    `props.points`, reusing the existing translate/scale helper pattern); (b) the container-bound text is
    recomputed via `computeBoundTextLayout(resizedContainer, textEl)` so it recenters/rewraps to the
    resized container — do NOT scale it by raw ratio; (c) independent group text scales x/y ONLY (new
    top-left), keeping `props.fontSize` unchanged (D-03). Return one flat `Element[]`. This is NEW math,
    NOT an extension of the single-element `resizeSession` (Pitfall 4).

    Add a group-resize session to `interaction.store.ts` mirroring `resizeSession` (store the group's
    old combined bbox from `getMultiSelectBounds` + the active handle). In
    `use-whiteboard-pointer-handlers.ts` / `pointer-down.ts`, when a handle pointer-down happens while
    the selection resolves to a group, start the group-resize session instead of the single-element one.
    In `pointer-move.ts`, when a group-resize session is active, compute `newBounds` from the dragged
    handle + pointer (bbox corner/edge math), call `computeGroupResizeDrafts(...)`, and
    `setDraftElements(...)`. `pointer-up.ts` commits the drafts via the existing one-`updateElements`
    branch and clears the group-resize session.

    Add a `GroupSelectionOverlay` to `SelectionOverlay.tsx`: reuse `MultiSelectionOverlay`'s dashed
    `#3b82f6` outline AND add the 8 handles (`nw,ne,sw,se,n,s,e,w`) with the EXACT single-element handle
    visual spec (`r=4`, `fill="white"`, `stroke="#3b82f6"`, `strokeWidth=1.5`, `cursor:'pointer'`),
    wired to `onHandlePointerDown`. Do NOT render a rotate handle (out of scope). In
    `LocalSelectionLayer.tsx`, render `GroupSelectionOverlay` (with handles) when the multi-select
    resolves to a shared group; keep the plain outline-only `MultiSelectionOverlay` for ad-hoc
    multi-select (no groupId) unchanged.

    Create `group-resize.test.ts` covering AC-9: non-text scaling, bound-text recenter/rewrap,
    independent-text position-only (assert fontSize unchanged — D-03), locked member unchanged, one
    commit batch.
  </action>
  <acceptance_criteria>
    - `computeGroupResizeDrafts` scales non-text members and leaves locked members unchanged. @covers AC-9 (D-02)
    - Container-bound text is recentered/rewrapped via `computeBoundTextLayout`, not raw-ratio scaled. @covers AC-9
    - Independent group text keeps its `props.fontSize` and only moves position (assert fontSize equality before/after). @covers AC-9 (D-03)
    - Group resize commits exactly one `updateElements` call. @covers AC-9 AC-12
    - `GroupSelectionOverlay` renders 8 handles and no rotate handle, only for group selections; ad-hoc multi-select stays outline-only. @covers AC-9 (D-01)
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter whiteboard-fe test -- src/canvas/tools/select/__tests__/group-resize.test.ts && pnpm --filter whiteboard-fe typecheck</automated>
  </verify>
  <done>Resizing the group bbox scales non-text members, recenters/rewraps bound text, and moves independent text without fontSize change, as one undo step.</done>
</task>

<task type="auto">
  <name>Task 3: AC-13 no-new-slot regression + phase test gate</name>
  <files>
    frontend/src/canvas/tools/select/__tests__/group-resize.test.ts (modify)
  </files>
  <read_first>
    - packages/shared/src/sync-contracts/field-mapping.ts (grouping.groupId slot L24-25 — must be unchanged)
    - packages/shared/src/sync-contracts/slot-validation.ts (grouping.groupId L34-35, L152-154)
    - packages/shared/src/sync-contracts/guards.ts (grouping.groupId L32-33, L98)
    - packages/shared/src/sync-contracts/types.ts (grouping.groupId slot type L31-32, L60-61)
    - packages/shared/src/index.ts (Element — confirm no groupId-binding field added)
    - .planning/phases/02.57-p2-5-07-merge-items-bind-text/02.57-RESEARCH.md (AC-13 confirmed: no new WS event, reuse grouping.groupId)
    - specs/048-merge-items-bind-text/acceptance.md (AC-13)
  </read_first>
  <action>
    Add an AC-13 regression assertion (a small test block, e.g. appended to `group-resize.test.ts` or a
    tiny dedicated `sync-contracts-unchanged.test.ts`) that fails if any new sync slot or WS event was
    introduced this phase. Concretely: assert the shared `WS_EVENTS` map and the `grouping` slot key set
    contain exactly the pre-existing keys (import from `@vdt/shared`), and assert that `ElementProps`
    exposes no `boundContainerId`/`isBoundText`-style field (a compile-time/`typeof`-level check or a
    keys assertion). Then run the FULL frontend suite as the phase gate. Also `git diff --stat` on
    `packages/shared/src/sync-contracts/` must show no changes attributable to this phase (verify no
    slot/guard/mapping edits were needed). Document the check in the SUMMARY.
  </action>
  <acceptance_criteria>
    - A regression test asserts no new WS event key and no new `grouping` slot key were added, and no bound-text field exists on `ElementProps`. @covers AC-13
    - `git diff` shows `packages/shared/src/sync-contracts/*` unchanged by this phase. @covers AC-13
    - Full suite `pnpm --filter whiteboard-fe test` is green (phase gate).
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter whiteboard-fe test</automated>
  </verify>
  <done>No new WS event or sync slot was introduced; groupId reuses grouping.groupId; full frontend suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Group drag/resize emit batched element updates through the existing `updateElements` path (existing x/y/width/height/props/zIndex slots). No new field, event, or endpoint (AC-13). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02.57-03 | Tampering | Group drag/resize batch honoring D-02 locked exclusion | low | mitigate | `computeGroupDragDrafts`/`computeGroupResizeDrafts` filter out `locked` members so a locked element cannot be moved/scaled by a group op; covered by unit tests. |
| T-02.57-13 | Information Disclosure | Accidental new sync slot leaking binding state | low | mitigate | AC-13 regression test asserts no new WS event / sync slot / element field added this phase. |
| T-02.57-SC | Tampering | npm/pip/cargo installs | n/a | accept | No packages installed this phase (RESEARCH Package Legitimacy Audit: not applicable). |
</threat_model>

<verification>
- `pnpm --filter whiteboard-fe test -- src/canvas/tools/select/__tests__/group-drag.test.ts src/canvas/tools/select/__tests__/group-resize.test.ts` green.
- Phase gate: `pnpm --filter whiteboard-fe test` (full suite) green.
- `pnpm --filter whiteboard-fe typecheck` and `pnpm --filter whiteboard-fe lint` clean.
- `git diff --stat packages/shared/src/sync-contracts/` shows no changes from this phase (AC-13).
- Manual (UI-SPEC): click a grouped element → combined bbox + 8 handles, no rotate handle; drag any member → whole group moves; drag a corner handle → group scales, bound label recenters/rewraps, independent text keeps font size; locked member stays put.
</verification>

<success_criteria>
- Clicking a grouped element selects the whole group with a handled bbox (D-01).
- Dragging any member moves every non-locked member as one batch (AC-8, D-02).
- Group-bbox resize scales non-text, recenters/rewraps bound text, and translates independent text without fontSize change (AC-9, D-03).
- Group move and group resize are each single undo/redo steps (AC-12).
- No new WS event or sync slot introduced (AC-13).
</success_criteria>

<output>
Create `.planning/phases/02.57-p2-5-07-merge-items-bind-text/02.57-03-SUMMARY.md` when done.
</output>
