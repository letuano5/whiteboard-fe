# Summary: Plan 05.2-01

## Completed

- Added shared P5 sync contract exports under `packages/shared/src/sync-contracts/`.
- Defined `SyncSlot`, `SlotPatch`, `SyncCommand`, command envelopes, order hints, read
  preconditions, and `CommittedChangeSet`.
- Added exhaustive `Element` and `ElementProps` field-to-slot/classification registries.
- Added shared validators for command envelopes, slot patches, duplicate slots, direct order
  patching, `isDeleted` patching, create duplicates, and tombstone-window creates.
- Reconciled revised `docs/SPECS.md` P5-02 details: `ArrowEndpointBinding`,
  `SlotReadPrecondition.onStale`, `baseClock` stale validation, command-level-only request/ACK
  rules, revised `UpdateArrowBindingCommand`, and `manual_replace`.
- Added `materializeCreatedElement` to represent server-materialized create output and
  normalized order.
- Added AC-tagged backend tests importing the public contracts from `@vdt/shared`.
- Split native file validation out of `packages/shared/src/index.ts` into `native-file.ts` to
  keep shared package files within the repo file-size guardrails while preserving public exports.

## Deferred

- P5-03 owns wiring these contracts into the backend `SyncRoom` execution path.
- Slot clock persistence, idempotency storage, actual ACK delivery, actual rebase execution,
  reconnect diff, and frontend reconciliation remain deferred to later P5 slices.
