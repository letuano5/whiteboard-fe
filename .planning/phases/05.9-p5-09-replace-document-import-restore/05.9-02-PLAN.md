# Plan 05.9-02: Frontend ROOM_REPLACED reconciliation and verification

Status: Completed 2026-07-02

## Scope

Add client-side `ROOM_REPLACED` reconciliation so peers wipe pending optimistic state, hydrate from
the authoritative replacement payload, update clocks/epoch, and ignore stale ACKs from pre-replace
requests.

## Target Files

- `frontend/src/sync/socket/p5-reconciliation.ts`
- `frontend/src/sync/socket/event-handlers.ts`
- `frontend/src/sync/socket/state.ts`
- `frontend/src/sync/socket/p5-reconciliation.test.ts`
- `.planning/phases/05.9-p5-09-replace-document-import-restore/VERIFICATION.md`
- `.planning/ROADMAP.md`
- `.planning/STATE.md`

## Implementation Slices

1. Add `applyRoomReplaced` reconciliation helper:
   - Clear pending queue.
   - Replace element store from payload.
   - Hydrate known slot clocks.
   - Set `lastServerClock` and `roomEpoch`.
   - Record cleared request IDs as stale so later ACKs are ignored if they arrive.
   - Verification: AC-tagged frontend test.

2. Register `WS_EVENTS.ROOM_REPLACED` in socket event handlers.
   - Verification: socket reconciliation tests.

3. Final verification and GSD closeout.
   - Verification: commands below.

## Acceptance-Test Tasks

- `AC-6`: client receiving `ROOM_REPLACED` clears pending, hydrates authoritative state, updates
  clock/epoch, and ignores old ACKs.

## Verification Commands

- `.agents/skills/implement-feature-gsd/scripts/check-ac-coverage.sh specs/038-p5-09-replace-document-import-restore/acceptance.md frontend/src/sync/socket/p5-reconciliation.test.ts`
- `pnpm --filter whiteboard-fe test -- p5-reconciliation`
- `pnpm typecheck`

## Risks And Deviation Handling

- Risk: full P5 command migration is still pending. Mitigation: keep replacement reconciliation
  isolated to `ROOM_REPLACED` and existing P5 state helpers.
