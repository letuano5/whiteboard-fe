# Plan 05.6-03: Enforce socket ACK safeguards and close P5-06 verification

## Scope

Update the realtime `SYNC_COMMAND` handler so only domain rejects emit reject ACKs. Persistence
failures, conditional clock conflicts, and unhealthy-room recovery failures must not ACK or
broadcast the command. Then run AC coverage and package validation.

## Target Files

- `backend/src/realtime/handlers/sync-command.ts`
- `backend/src/realtime/handlers/sync-command.test.ts`
- `.planning/phases/05.6-p5-06-transactional-persistence-idempotency/VERIFICATION.md`
- `.planning/ROADMAP.md`
- `.planning/STATE.md`

## Implementation Slices

1. Preserve existing domain reject ACK behavior for viewer/permission/validation errors.
   - Verification: existing handler tests.

2. Suppress ACK/broadcast/dirty side effects when `SyncRoom` reports `ROOM_UNHEALTHY` or when
   persistence failures bubble out without a committed result.
   - Verification: handler tests for no ACK/broadcast/dirty on DB fail and conditional clock fail.

3. Run final AC coverage, Prisma validation/generation, backend focused tests, and backend
   typecheck. Record verification status in GSD artifacts.
   - Verification: commands below.

## Acceptance-Test Tasks

- `AC-3`: handler duplicate replay still ACKs caller once per attempt but broadcasts only original commit.
- `AC-6`: handler emits no ACK/broadcast/dirty state when persistence fails before commit.
- `AC-8`: handler emits no ACK/broadcast/dirty state when conditional clock conflict marks room unhealthy.

## Verification Commands

- `.agents/skills/implement-feature-gsd/scripts/check-ac-coverage.sh specs/035-p5-06-transactional-persistence-idempotency/acceptance.md backend/src/sync/sync-room-persistence.test.ts`
- `pnpm --filter whiteboard-be test -- sync-room-persistence sync-room sync-command shared-contract-invariants room-repository`
- `pnpm --filter whiteboard-be prisma generate`
- `pnpm --filter whiteboard-be prisma validate`
- `pnpm --filter whiteboard-be typecheck`

## Risks And Deviation Handling

- Risk: suppressing ACKs for unexpected failures can look like a timeout to clients. Mitigation:
  this is intentional for P5-06 persistence ambiguity; resend/reconnect recovery uses persisted
  idempotency and diff/snapshot paths.
