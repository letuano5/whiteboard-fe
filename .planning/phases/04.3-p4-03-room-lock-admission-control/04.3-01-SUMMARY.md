---
phase: '04.3'
plan: '04.3-01'
subsystem: 'room-access'
tags: ['p4-03', 'capacity-control', 'presence']
key-files:
  - 'packages/shared/src/index.ts'
  - 'backend/src/rooms/room-roles.ts'
  - 'backend/src/realtime/handlers/join-room.ts'
  - 'backend/src/realtime/handlers/element-update.ts'
  - 'frontend/src/rooms/CapacitySettings.tsx'
  - 'frontend/src/components/ui/OnlineUsersPanel.tsx'
---

# Plan 04.3-01 Summary

## Completed

- Added `maxParticipants` and `maxEditors` room metadata to Prisma schema and migration.
- Extended shared room access and presence contracts with capacity, base role, and effective role
  fields.
- Implemented admission-aware access resolution:
  - `maxParticipants` rejects additional joins with `room-access/room-full`.
  - `maxEditors` downgrades eligible editors to `effectiveRole = 'viewer'`.
- Stored `baseRole` and `effectiveRole` in socket data and room presence.
- Kept server-side mutation authorization authoritative for downgraded viewer sockets.
- Added owner-only HTTP capacity update for `maxParticipants` and `maxEditors`, with upper-bound
  and cross-field validation.
- Added Share modal capacity controls and online-users role display.
- Added AC-tagged backend/frontend tests for all P4-03 acceptance criteria.

## Verification

- AC coverage guard: passed for AC-1 through AC-6.
- `pnpm test`: passed.
- `pnpm lint`: passed.
- `pnpm typecheck`: passed.
- Prettier check for touched P4-03 files: passed.

## Deviations

- Room-level access lock/unlock was removed from P4-03 after product review; `Room.locked` remains
  only as document metadata outside the Share/access runtime.
- Realtime queue/auto-promotion remains out of scope; implementation supports rejoin or reload to
  claim newly available editor slots.

## Self-Check

PASSED. Plan tasks are implemented and verified against the acceptance registry.
